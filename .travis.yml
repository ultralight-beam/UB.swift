os: osx
language: swift
osx_image: xcode10.3
xcode_project: UB.xcodeproj
xcode_scheme: UB.Package

jobs:
  fast_finish: true
  include:
    - script: make test
    - script:
        - make xcode
        - make lint
      env: ALLOW_FAILURE=true
  allow_failures:
    - env: ALLOW_FAILURE=true

notifications:
  email: false
